# Commands to download dependencies locally

```
npm install nodemon --save-dev
```
```
npm i express morgan cors
```
```
npm install bcrypt
```
```
npm install jsonwebtoken
```

Please do it inside the api folder, not outside at the same level as the ui and api since there is no .gitignore file there that hides the node_modules.
